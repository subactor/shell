# Bounded intent: performance dashboard CLI

Expose the read-only Ops Observer `/api/process-costs` projection through the
one-shot `performance`/`perf` command and interactive `/performance` command.
Use a separately validated observability origin, never accept a caller URL,
never mutate or widen URI Process authority, and preserve JSON output.
